package org.joinfaces.resume.cv.logo.repository;

import org.joinfaces.resume.common.repository.AbstractRepository;
import org.joinfaces.resume.cv.logo.entity.LogoEntity;
import org.springframework.stereotype.Repository;

@Repository
public interface LogoRepository extends AbstractRepository<LogoEntity> {
}
